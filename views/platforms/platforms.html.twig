<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
        <link rel="stylesheet" href="style.css">
        <link rel="stylesheet" href="https://bootswatch.com/4/darkly/bootstrap.min.css">
        <title>Liste des Consoles</title>
    </head>
    <body>
        <div class="container">
            <nav class="container navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
                <a class="navbar-brand" href="/">Games-Catalog</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                  <span class="navbar-toggler-icon"></span>
                </button>
              
                <div class="collapse navbar-collapse" id="navbarColor01">
                  <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                      <a class="nav-link" href="/">Accueil <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                      <a class="nav-link" href="/platforms">Les Consoles</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/games">Les Jeux</a>
                      </li>
                  </ul>
                </div>
              </nav>
            <h1 id="titreConsoles" class="text-center">Les Consoles de jeux</h1>
            <div class="row mb-5">
                {% for platform in platformList %}
                <div class="col-md-4 mx-auto pl-5">
                    <div class="card mb-5 d-flex align-items-stretch" style="width: 18rem; height: 600px;">
                        <img src={{ platform.image }} class="card-img-top" alt="..." height="200rem">
                        <div class="card-body">
                            <h5 class="card-title">{{ platform.name }}</h5>
                            <p class="card-text">{{ platform.description }}</p>
                        </div>
                        <div class="card-footer">
                            <a href="#" class="btn btn-primary bottom btn-block">Voir les Jeux</a>
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>
        <footer>
            <div class="container card-footer bg-primary text-white mt-5 fixed-bottom">
                        Games-Catalog © 2020
            </div>
        </footer>
    </body>
</html>